#include <Arduino.h>

#include <math.h>

float convertHpaToPa(float);
float convertPaToHpa(float);
float convertHpaToAtm(float);
float convertAtmToHpa(float);
float convertHpaToTorr(float);
float convertTorrToHpa(float);
float convertHpaToHg(float);
float convertTorrToHg(float);