#include <Arduino.h>

#include "const.h"

String processorRain(const String &var)
{
    if (var == "RAINCHANCE")
    {
        return "";
    }
    else if (var == "RAINVOLUME")
    {
        return "";
    }
    return String();
}