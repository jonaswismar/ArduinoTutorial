#include <Arduino.h>

float calculateDewPoint(float, float);
float convertCelsiusToFahrenheit(float);
float convertFahrenheitToCelsius(float);
int calculateBeaufort(float);
int calculateFujita(float);
int calculateEFujita(float);
int calculateSaffirSimpson(float);
int calculateTorro(float);
float calculateHumidityAbsolut(float, float);